using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace ThemeApi
{
    // This is the only MonoBehaviour among all theme API, and is
    // responsible for running Update event handlers and
    // coroutines.
    public class FrameEventRunner : MonoBehaviour
    {
        void Start()
        {
        }

        void Update()
        {

        }
    }
}