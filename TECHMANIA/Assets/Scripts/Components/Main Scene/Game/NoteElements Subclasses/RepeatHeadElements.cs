using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;

public class RepeatHeadElements : RepeatHeadElementsBase
{
    public RepeatHeadElements(Note n) : base(n) { }

    // That's the whole class.
}
