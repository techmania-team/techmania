using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Contains common methods between HoldNoteAppearance,
// RepeatHeadHoldAppearance and RepeatHoldAppearance.
//
// Sadly interfaces don't allow instance fields, so they
// are defined in NoteAppearance.
public class IHoldAppearance : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
